import { Story } from '@storybook/react/types-6-0';
import React from 'react';

import { {{ pascalCase name }}, {{ pascalCase name }}Props } from './{{ kebabCase name }}';

export default {
  title: 'Category / {{ titleCase name }}',
  component: {{ pascalCase name }},
};

const Template: Story<{{ pascalCase name }}Props> = (argumentz): React.ReactElement => (
  <{{ pascalCase name }} {...argumentz} />
);

export const Default = Template.bind({});
Default.args = {
  children: 'Lorem Ipsum',
};
