import { Story } from '@storybook/react/types-6-0';
import React from 'react';

import { {{ titleCase name }}, {{ titleCase name }}Props } from './{{ kebabCase name }}';

export default {
  title: 'Category / {{  titleCase name }}',
  component: {{  titleCase name }},
};

const Template: Story<{{  titleCase name }}Props> = (argumentz): React.ReactElement => (
  <{{  titleCase name }} {...argumentz} />
);

export const Default = Template.bind({});
Default.args = {
  children: 'Lorem Ipsum',
};
